services:
   postgres:
     container_name: postgres-sql-ZenFin
     image: postgres

     environment:
       POSTGRES_USER: chethan_gowda
       POSTGRES_PASSWORD: purva@1927
       GDATA: /var/lib/postgresql/data
       POSTGRES_DB: ZenFIn_DB
     volumes:
       - postgres:/data/postgres
     ports:
      - 5432:5432
     networks:
      - spring-demo
     restart: unless-stopped
   mail-dev:
     container_name: mail-dev-ZF
     image: maildev/maildev
     ports:
       - 1080-1080
       - 1025:1025

networks:
  spring-demo:
    driver: bridge
volumes:
  postgres:
    driver: local